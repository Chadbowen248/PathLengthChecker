<Application x:Class="PathLengthCheckerGUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="Application_Startup"
             Exit="Application_Exit">
  <Application.Resources>
    <SolidColorBrush x:Key="AppBackground" Color="#F3F5F9"/>
    <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#2563EB"/>
    <SolidColorBrush x:Key="AccentBrushHover" Color="#1D4ED8"/>
    <SolidColorBrush x:Key="MutedText" Color="#64748B"/>
    <SolidColorBrush x:Key="BorderBrushSoft" Color="#E2E8F0"/>
    <SolidColorBrush x:Key="PrimaryText" Color="#0F172A"/>
    <SolidColorBrush x:Key="InputBackground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="InputForeground" Color="#0F172A"/>
    <SolidColorBrush x:Key="GridBackground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="GridAltRow" Color="#F8FAFC"/>
    <SolidColorBrush x:Key="SecondaryButtonBackground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SecondaryButtonForeground" Color="#0F172A"/>

    <Style TargetType="Window">
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Background" Value="{DynamicResource AppBackground}"/>
      <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
    </Style>

    <Style TargetType="GroupBox">
      <Setter Property="Padding" Value="12"/>
      <Setter Property="Margin" Value="0,0,0,10"/>
      <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
    </Style>

    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="Padding" Value="6,4"/>
      <Setter Property="Margin" Value="0,2"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
      <Setter Property="Foreground" Value="{DynamicResource InputForeground}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
      <Setter Property="CaretBrush" Value="{DynamicResource InputForeground}"/>
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="Padding" Value="6,4"/>
      <Setter Property="Margin" Value="0,2"/>
      <Setter Property="MinHeight" Value="28"/>
      <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
      <Setter Property="Foreground" Value="{DynamicResource InputForeground}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="Margin" Value="0,4,12,4"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
      <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="16,10"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AccentBrushHover}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button">
      <Setter Property="Padding" Value="12,8"/>
      <Setter Property="Margin" Value="0,0,8,0"/>
      <Setter Property="Background" Value="{DynamicResource SecondaryButtonBackground}"/>
      <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonForeground}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style TargetType="DataGrid">
      <Setter Property="RowHeight" Value="26"/>
      <Setter Property="GridLinesVisibility" Value="Horizontal"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
      <Setter Property="Background" Value="{DynamicResource GridBackground}"/>
      <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
      <Setter Property="AlternatingRowBackground" Value="{DynamicResource GridAltRow}"/>
      <Setter Property="RowBackground" Value="{DynamicResource GridBackground}"/>
      <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrushSoft}"/>
    </Style>
  </Application.Resources>
</Application>
